# 个人网站
- 这个是我的个人网站，我不希望开放注册，而是只允许我登录
- 网站中不要体现“站长”这样的身份。对于一些网站的使用约束，不需要体现在网站中（比如这个网站只允许站长登录）
- 加个gitignore文件
- 使用方式写到README里
- 网站支持深色浅色两种主题，并根据当前时间自动切换主题。
- 网站有以下身份：admin,insider,guest。insider是介于游客和管理员之间的身份，可以浏览一些游客没有权限浏览的内容。
- 项目提供服务器部署方法
- 网站需要提供搜索功能，用于站内查找
- 网站要有一键回到顶部的功能
- 访问有问题时，网站会返回错误码对应的图片
- 网站显示适配高分辨率显示器
- 整个网站按照ios那样的非线性动画进行设计
- 网站图标使用 iOS 线性 SF Symbols 风格
- 优化加载体验，等待时间较长时显示等待进度提示


# 桌宠
- 参考这个项目，获取桌宠并做到网站里。https://github.com/QCYTSN/dsh-dafeiyu
- 默认inside可见。管理员可以在设置里配置权限


# 主页
- 主页显示公告
- 当游客输入inside的时候，获取这个权限。管理员可以在设置中配置网页内容访问权限
- 进入inside模式后，网站有壁纸背景图


# 登录系统
- 隐藏登录入口，只在键盘敲"login"的时候弹出登录窗口
- login弹窗出来后，应该在用户名处有键入光标
- 给密码框加个点击能查看密码明文的功能
- 登录之后右上角不要显示用户名，改成显示"网站版本.commit_id"


# 网页0 setting
- 需要登录才有
- 登录之后多出来一个设置的页面，设置功能支持设置页面访问权限，即设置哪些网页对游客显示
- 网站壁纸、anime成人内容，也在设置中做成像页面访问权限一样可配的
- 设置页面中我希望看到服务器监控。CPU使用率、内存使用情况，网络带宽出网、入网，系统盘写、读，需要做个监控图表显示详情，可以选择按小时、天、周、月、自定义来显示
- 我希望给管理员加个升级网站的功能：
  - 当检测到有新版本时，提示可以升级，并告诉我落后了新版本多少个提交记录等信息
  - 考虑到执行更新脚本本身也需要root用户权限，在管理员点击更新，弹出输入su密码的提示
  - 如果版本校验和新版本一致，则无需升级，可以给出提示并让按钮变灰


# 控制台
- 仅管理员可用
- 使用模拟器终端，例如xterm.js，支持在终端内查找功能
- 支持多终端
- 全宽 + 全高铺满页面，不要有空隙，支持中文字符正常显示
- 用于访问服务器后端服务器
- 支持文件管理功能，可以下载、上传、编辑文件，并支持新标签页查看html文件


# 网页1 blog
- 游客仅能访问
- 管理员可以设置每个blog是否对游客隐藏
- 需要有新建功能
- 需要有在线编辑功能，支持文字和图片
- 修改功能支持修改标题和内容
- 提供置顶博客的功能，置顶的那篇作为公告
- 支持上传图片、文字


# 网页2 wiki
- 游客仅能访问
- 管理员可以设置每个wiki是否对游客隐藏
- 需要有新建功能
- 需要有在线编辑功能，支持文字和图片
- 修改功能支持修改标题和内容
- wiki编辑支持的类型：markdown、所见即所得、html
- wiki条目信息做成右侧停靠隐藏的，鼠标移动过去自动出现。wiki内容的标题导航也加到这里，并且高亮导航中当前所在的小结
- html类wiki在网页主体中显示原生样式，网站顶部导航留着
- 正文编辑功能里加个编辑栏.里面可以给我选种的文本配置样式，比如字体大小、颜色、代码片段等等
- 支持上传图片、文字


# 网页3 anime
- 做一个动漫日历，能够获取当前档期的动漫，并在日历上显示更新时间，时间精确到分钟
- 日历列宽要一样
- 把动漫的封面也显示在日历条目上，鼠标移动上去显示的预览封面可以大点，注意封面比例要对
- 不要折叠，所有动漫都显示在格子里
- 日历可以按周显示，格子大点
- 加个语言选择功能，默认中文，用于支持动画名中文显示，以及番剧详情的中文描述
- 加个缓存，避免重复请求，当服务器中有数据时，不从数据源网站请求
- 网页背景使用本地图片，从指定目录下随机选择


# 网页4 tools
- 默认对游客可见
- 子功能做到二级页面

## json格式化
- JSON 格式化原文结果做成两列显示
- 从网页整体布局的角度，优化显示行数。显示结果的文本框，要尽可能多的显示行数

## 二维码解析

## 生成二维码
- 效果实现参考：https://recent.design/i/3driga1-animated-qr-code-morphing
- 输入 URL/文本，生成 3D 二维码体素树
- 树底下要有嫩绿色二维码草坪，二维码草坪也应该是 3d 样式的方块，只包含二维码本身的内容，不需要 padding
- 树要有树干，要细一点，不适过短
- 树冠要大一点，颜色樱花粉
- 更新字符串时不需要过渡动画，直接替换成新的渲染好的二维码树
- 3d 的二维码树点击后切换成俯视角，再次点击切换成 3D 树，有过渡动画
- qr-tree 中不需要再显示黑白的二维码图片，以及压平等信息
- 树的视角要保证3d时看到的是45度俯视视角，正面方向是二维码的角。选择的方位在此基础上保证2d/3d切换时，旋转角度最小

## 图片裁切
- 对于上传的版图，可以调整网格线，裁切成一张张小图
- 等分预设中允许用户自定义
- 切割线可以删减
- 支持自动按框裁切，注意：1. 图片可能不是等分的 2. 框可能很粗

## 图片拼接
- 支持用户上传多张图片进行拼接

## HTML渲染
- 用户传入html代码，点击渲染后在新标签页中打开，本地/服务器不存留渲染的网页

## 图片裁切
- 对于上传的版图，可以调整网格线，裁切成一张张小图
- 等分预设中允许用户自定义
- 切割线可以删减

## 文本对比
- 仿照beyond compare实现，描述中不要体现beyond compare
- 网页两侧不要空着，text-input要把网页宽度占满

# 网页5 game
- 游戏页面对游客公开
- 游戏为 Shattered Pixel Dungeon 网页版（TeaVM/libGDX 移植）
- 游戏本体嵌入 /spd/index.html，GameView 使用全屏 iframe 承载
- 构建产物位于 public/spd，来源与重建方式见 game/README.md
- 支持鼠标 / 键盘操作，依赖 WebGL
