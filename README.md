# 🗓️ AniHub —— Anime · Blog · Wiki

全栈多页面网站：**Anime**（当前档期放送时间表，精确到分钟）+ **Blog**（网站更新日志 / 随手记录）+ **Wiki**（笔记，支持 Markdown / 所见即所得 / HTML 文档）+ **Tools**（工具箱）。登录后即可写作，深浅主题按时间自动切换。

数据来源为 [AniList](https://anilist.co) 公开 GraphQL API，无需注册或 API Key。

## 功能

### 视觉与交互

- **导航栏毛玻璃（frosted glass）**：`backdrop-filter` + 半透明面板底色（无渐变，通体均匀）+ 顶部 `inset 1px` 内高光与底部极淡描边；滚动后投影加重形成「浮起」的层次。玻璃参数集中在 `:root` 的 `--nav-bg-alpha` / `--nav-blur` / `--nav-sat`，便于统一微调（**不要在 `.navbar` 里重复定义**，否则会盖掉 `:root` 的值）
  - **页面在顶部时不要「上深下浅」**：此时玻璃身后就是壁纸本身，壁纸顶部偏暗就会透出一条暗色带。实测把 `--nav-bg-alpha` 提到 82%、模糊加到 28px 可把导航栏内部上下亮度差从 **-63 压到 -25**（深色主题 22 → 6.4），同时保持底部与页面背景的融合（差 3.3）。`--nav-sat` 保持接近 1：饱和度提升会放大壁纸色相，浅色主题下容易发蓝
  - **高度（桌面单行 48.5px）由两处决定**：`.navbar` 的上下 `padding`（8px）与最高的子元素。实测导航链接（`.links a` 上下内边距 5px → 31px）与搜索框（31px）齐平才是关键 —— 之前链接是 35px、把整条顶栏撑到 56px。调高度时先量「哪个子元素最高」，只压 padding 或只压链接都可能被另一项顶住
  - 高度变化会自动通过 `ResizeObserver` 写进 `--nav-h` / `--wp-shift`（用 `offsetHeight`，整数），阅读页 / 拓扑图 / 控制台等「铺满整屏」的布局都据此贴合，无需各自改常量
- **壁纸独立图层**：全站壁纸从 `body::before` 迁到 `src/components/WallpaperLayer.vue`（`.app-shell` 内 `z-index:-1` 的固定图层）。原因是毛玻璃必须能糊到**身后的真实内容**，而 `body::before` 在 `.app-shell` 层叠上下文之下，导航栏透过它只能看到底色、模糊出来是一片纯色。迁移后壁纸仍在所有内容之下，但导航栏能糊到它 —— 这是玻璃感成立的前提
- **Wiki 拓扑（深空场景）顶栏必须与其它页面一致**：导航栏在所有页面、两种主题下都用同一套主题变量（`--panel` / `--text` 等），**不做任何按场景切换配色的特例**。顶栏颜色来自「半透明玻璃 + 身后的壁纸」，所以实测应各页完全相同（浅色主题 `#A0BEDF`、深色 `#1D3554`）。
  - 两个曾经的错误做法（都会让顶栏变成另一块纯色）：
    1. 给导航栏加 `.on-deep-space` 固定深色玻璃 → 浅色主题下顶栏近黑 `rgb(16,22,42)`
    2. **往 `<html>` 刷深空底色**（`html.style.backgroundColor = '#0b1322'`）→ 全站壁纸是 `.app-shell` 内 `position:fixed; z-index:-1` 的图层，会被 `<html>` 的背景整个盖住，而导航栏的 `backdrop-filter` 只糊它正后方的内容 → 顶栏从「壁纸 + 玻璃」退化成「深空纯色 + 玻璃」，浅色主题下变成近白 `#F8F9FC`
  - 深空背景由拓扑区域自己负责：`.graph-full` / `.graph-sky` 是不透明的 `#0b1322`，且该视图高度 = 视口高 − 导航高、不产生文档滚动，因此不需要给 `<html>` 兜底也不会露白边。`colorScheme` 仍保留（只影响原生滚动条/表单控件配色，不参与背景绘制）
  - 回归断言里包含「任何路由都不得往 `<html>` 写内联 background-color」这一条，防止再次踩坑
- **首页背景：真实 88 星座星空（`ConstellationField.vue`）**：星点按**真实星座**的位置落位，同一折线内的相邻星点连线，另加一层很淡的碎星填缝。
  - **数据**：`src/data/constellations88.js` —— 从 d3-celestial 的 `constellations.lines.json` 生成（BSD-3-Clause，见 `scripts/build-constellations.mjs`），含 88 星座 / 150 段连线 / 893 个点。运行时不需要联网，产物随仓库提交；要更新数据跑 `node scripts/build-constellations.mjs`
  - **预处理**做了四件事：① 按 id 归并（Ser 在源数据里被拆成两个 feature）；② **RA 环绕（unwrapping）** —— 星座跨过 RA 0° 时（大熊、室女等）直接取差会得到"横跨整个天区"的假跨度，实测把 Cha 压成 `w=1/h=0.01` 的一条细线，处理方式是把最大的那个空隙找出来、其余点 +360 搬回来；③ 等比归一化到 [0,1] 并居中（真实角尺寸差 10 倍以上，不统一的话小星座看不见）；④ 给极扁的星座（南天的 Oct/Aps/Cha，真实比值只有 0.05）一个 **0.3 的宽高比下限**，轻微非等比拉伸把它撑开，否则放进格子是一根头发
  - **摆放**：每个星座带自己的 `w/h`，按格子尺寸分别缩放，形状不会被压扁拉长。格子约 4.2 万 px²，1400×900 摆 24 个、1920×1080 摆 48 个左右；88 个**不重复地**铺一遍，不够再从头循环并换抖动/缩放/角度
  - **密度踩过的坑**：① 早期把数量上限卡在星座总数（当时只有 6 个），1400×900 只摆得出 3 个；② 只摆 `want` 个时**最后一行常常只占一两格**，那个角落就空着（实测 4×3 分区里出现 0），现在行数定下来后把数量**补满整行**（`n = cols × rows`）；③ 换成 88 星座数据后**密度得收紧** —— 这份数据比原来手写的 6 个稀疏得多（总共 150 段、平均每个星座不到 2 段），格子从 11 万缩到 4.2 万；但也试过 3 万，画面碎成一片、看不出星座形状
  - 连线线宽 1.45、不透明度 0.6（比 6 星座时代粗/亮），因为 88 星座单段太少，太细整片星空看着是散的
  - 星座整体随时间**极慢自转 + 轻微呼吸缩放**（幅度 3.5% / 0.05 rad），形状始终可辨识，但不像贴图那样死板
  - 亮星的光晕用**径向渐变**：先前用"实心圆 + 低透明度"会渲染成有明显硬边的圆盘，亮星看起来像贴了个圆片
  - **去掉了鼠标交互**：早先版本会让靠近光标的粒子被吸引并提亮，视觉上很吵。现在鼠标完全不参与（连监听都不装），只有滚动视差与自身动效
  - 新增 `canvas.dataset.rebuild` 重建标记：切页面回来号不变即证明星空未被重排。**这个标记是必要的** —— 试图用像素比对判断"是否重排"会被闪烁噪声淹没（原地等 9s 的 IoU 只有 32%，导航往返 12.5%，根本分不出来）
- **首页 Logo：三星系统（`HomeLogoMark.vue`）**：三颗星走 **O₂(1.2) 周期轨道** —— 一般三体问题的一条"编舞"周期解，三颗星沿交叉的闭合曲线互相追逐（参考图来自 Li et al. 2023 "New three-dimensional periodic orbits of the general three-body problem"）。
  - **初值**（照参考图，`G = 1`）：
    - 质量 `[1.0, 1.0, 1.2]`
    - 初始位置 `[(-1,0,0), (1,0,0), (0,0,1.02201)]`
    - 初始速度 `[(-0.2726,-0.432094,0.629473), (-0.2726,-0.432094,-0.629473), (0.454333,0.720156,0)]`
    - 周期 `6.90577639826182`
  - **为什么这版用数值积分**（上一版分层构型用的是开普勒解析式）：这是一条**一般三体的周期解**，没有"内层双星 + 外层伴星"那种可分解结构，写不出解析式；只能速度 Verlet 积分
  - ⚠⚠ **这条轨道不能一直积分下去 —— 它是"不稳定"的（混沌）**：
    - 实测（**两个独立步长给出相同的增长率**，所以这**不是积分精度问题**，是轨道本身的性质）：

      | 周期数 | 距初值偏差 (dt=1/50000) | 距初值偏差 (dt=1/200000) |
      |---|---|---|
      | 1 | 3.0e-5 | 3.4e-5 |
      | 4 | 1.17 | 1.17 |
      | 5 | 2.15 | 2.15 |
      | 6 | **204** | **44** |
      | 200 | 117310 | 4353 |

    - 放大率约 **10⁴ / 周期**。也就是说**哪怕把步长缩到 1/200000，几个周期后照样飞出去** —— 靠调步长救不了
    - 现象就是"跑一会儿变轨、最后飞出去"（用户报的就是这个）
    - ⚠ 我第一次验证只跑了**一个周期**（闭合 3.4e-5）就下了"精确周期"的结论 —— 对混沌系统这是**不够的**。长期验证必须跑几十个周期
  - **解法：离线制表 + 按相位查表播放**（不实时积分，也不"到点重启"）
    - 关键认识：解本身在数学上是精确周期的（P 之后精确回到初值），数值积分只是在 P 处留下 3.4e-5 的残差、而这个残差会被混沌放大。所以**只要一直在用"残差还没被放大"的那一段**就没问题
    - 做法：挂载时**把整整一个周期积分成一张高密度表**（`TABLE_HZ = 240` → 约 1658 项），此后画面只是按相位在这张表上走动；相位连续增长、到 1 回绕到 0
    - 为什么不是"每个周期重置回初值"（我先做的是那个）：那样**看得出来"每 7 秒重来一次"**（用户报的就是这个）。查表法没有"起点/终点"的概念，相位是连续绕圈的，所以**没有重启感**
    - 实测：重置法 300 个周期最大半径 **1.0709**（不重置是 **177675**）；查表法天然有界（就是一个周期循环）
    - 性能顺带更好：每帧只做一次线性插值，不再需要每帧跑 ~1600 步积分
  - **接缝为什么看不见**（下面两条都是踩出来的）：
    1. **位置必须跨表尾/表首插值**：只在表内相邻两项插值的话，相位回绕瞬间位置会跳 **~49 用户单位**（实测相邻帧最大位移 51）。改成"从末项插到首项"后降到 **2.5**（这就是轨道在 30Hz 采样下的正常弧长）
    2. ⚠⚠ **尾巴只需要老老实实记录"光点去过哪里"，不要做任何修正**：我曾为了"更顺滑"写过一段"让旧尾巴点按年龄权重追向新位置"的补偿逻辑 —— 结果它把整条尾巴往中心拽，缩成一团乱麻（**这就是用户报的"轨迹线不对"**）。
       - 实测：补偿版尾 0 的最大半径只有 **20.2**，而它真实轨道半径是 **28.5**；去掉补偿后尾巴包围盒 `[3.5,12.3,54.5,46.6]` 与引导环**完全一致**
       - 教训：位置本身在回绕处已经连续（见第 1 条），回绕前后同相位的点本来就几乎重合，**不需要额外补偿**
       - **判断"尾巴对不对"的可靠方法**：把「引导环包围盒」和「尾巴包围盒」分别量出来对比，两者应当**一致**；不一致就说明尾巴被改坏了
    - ⚠ 验收这类"接缝"时，**取样必须按稳定的 `is-cN` 类**，不能按 DOM 下标：三颗星每帧都按 depth 重排，按下标取会把"不同的星"当成同一个、量出**假的 51 单位跳变**（我第一次就是这么误判的）
  - ⚠ **位置要减掉质心**：原值下系统质心在 `z = 0.383254`，投影后会整体偏出 viewBox 中心、各天体轨迹也挤在一起。验证过**总动量本来就是 0**，所以**只需要挪位置、不需要改速度**（这是排查时差点搞错的地方 —— 一开始怀疑动量不守恒，实测发现动量是 0、偏的是质心）
  - **3D 透视**：这套初值本身是**三维**的（z 速度 ±0.629473，z 跨度与 x 同量级）。投影方式是 x 水平、y 按 `TILT_Y = 0.62` 压成竖直分量、z 一部分按 `TILT_Z = -0.46` 参与竖直方向（让"远"的一侧偏上、形成斜向椭圆）、另一部分作为**深度**决定近大远小与前后遮挡（`--z = 1 + depth·0.34`，按系统最大半径归一化）
  - **轨道引导环**：三颗星各画自己那一圈的完整轨迹 —— 也就是参考图里那三条交叉曲线。它和播放用的是**同一张周期表**（见上），所以环与实跑轨迹永远一致
  - **整套轨迹正好铺满 viewBox（自适应缩放）**：`SCALE` 不是手写常数，而是由**同一份离线采样**量出投影后的最大半径、再反算出来（`fitScale()`，`FIT_RADIUS = 28.5`）。这样改了视角/初值也不会忽然变大变小。
    - `FIT_RADIUS` 不能直接取 30（= 半宽）：圆点还要再往外探一点，取 30 会顶出 viewBox
    - 实测：轨迹直径 **118px / logo 132px = 89%**（此前只有 49/64 的 viewBox 占用、视觉上明显偏小）
  - **每颗星 = 外层柔光 + 内层小圆点**（需求变过几轮，最终形态如下）：
    - 内层 `.lm-dot`：实心小圆点，直径 **8~10px**，占轨迹直径（118px）约 **7~9%** —— 只是轨迹上的一个点，轨迹是画面主角
    - 外层 `.lm-dot-bloom`：柔光，直径 **25~32px**（约圆点的 3 倍），渐隐到 0。用户要"带点发光，像背景里的粒子一样"
    - ⚠ **柔光必须用"从中心渐隐到 0 的径向渐变"**，不能用实心圆 + 低透明度（那会渲染成有明显硬边的圆盘）。这和背景亮星是同一套手法、同一个坑
    - ⚠ 层级：bloom 画在 dot **下面**，圆点边缘才不会被柔光糊掉
    - 两个都有独立的呼吸动画，但这个呼吸**只动 opacity**；keyframes 里**不能写 transform**，否则会盖掉 `scale(--r)`（踩过这个坑）
    - 尺寸演进（需求反复）：大球 33px → 圆点 15px → 圆点 8~10px → **圆点 8~10px + 柔光 25~32px**
      - ⚠ 缩放系数只能**实测标定**：`scale(calc(var(--r) * k))` 算出来的矩阵值和 `r × k × --z` 对不上（实测差约 2.4 倍），照公式调会差很多。做法是改一次 k、量一次渲染像素
      - 圆点 k = 0.085（直径 8~10px）；柔光 k = 0.26（直径 25~32px）
    - 圆点用**各自的径向渐变**填（不是纯白）→ 三颗星仍是三种颜色
    - 圆点渐变的**最外圈不透明**（`stop-opacity=1`），边缘干净；柔光交给 `.lm-dot-bloom` 那层单独做
    - 整个 Logo 子树里**没有 `filter` / `box-shadow`**（`.lm-svg` 的整体投影、彗尾柔光都删了）—— 柔光是用渐变画的，不是 drop-shadow
  - **已按用户要求删除的四件装饰**（都曾让画面显得杂乱）：
    1. `.lm-glow` —— 铺在 132px 盒子**外面**的一圈大柔光（`inset: -14%` + 呼吸动画 + hover 再放大 1.12×）
    2. `.lm-dust` ×2 —— 两个碎星（用户看到的"上面的两个黑星"）。它是白色四角星 + 柔光，但在浅色/深色壁纸上都显得像黑点
    3. `.lm-orbit` —— `r=30.5` 的外圈虚线星轨（44s 自转，hover 提速到 9s）
    4. `.lm-shoot` —— hover 时沿外圈扫过的白色流星（用户说的"最外圈那个白色物体"）
    删除后 `lmBloom` 渐变、`lm-breathe` / `lm-spin` / `lm-shoot` / `lm-twinkle` 四组 keyframes 也一并清掉了
  - **彗尾**：每颗星拖一条由小段拼成的尾迹，逐段给透明度 —— 最老的一段渐隐到 0，看起来像在消逝。
    - **常态**：按弧长裁（`TRAIL_LEN = 150`），老段不断被挤掉
    - **点击**：`is-pinned` 后放宽到 `TRAIL_LEN_PINNED = 420`（≈ 一个半圈的弧长），跑满一圈就留下完整轨道；再点一次恢复消逝
    - ⚠ 尾巴长度按**屏幕弧长**限，不按点数、也不按时间：
      · 按时间限过 —— 帧率一被节流（rAF 在无头/后台会降频），同样时长里跑过的距离差很多，长度就飘（实测 328~620 段乱跳）；
      · 按点数限也不对 —— 点数与速度挂钩，同一个点数上限在快的时候是短尾、慢的时候能拖出好几圈（实测 1323 段把 logo 糊成一团）。
    - 采样步长也要**明显小于线宽**，否则逐段直线连不起来、尾巴看着是一串点
    - 三体编舞的轨迹是个**紧致的小环**（投影后整套系统只占约 50×34 单位），所以尾巴弧长要比分层构型（当时 72/210）给得长得多，才看得出"三条交叉的环"
    - **三条轨迹必须明显异色**（用户要求"轨迹颜色要不一样"）：
      - 之前三条是 `210,228,255 / 160,205,255 / 196,214,255` —— 全是浅蓝、只差几个色阶，叠在一起分不出谁是谁（而且第 0 条和第 2 条是同一色系、顺序还和圆点对不上）
      - 现在每条用**自己圆点的主色相**，并把亮度调到接近（紫罗兰按原亮度会明显比另两条暗，特意提亮）：
        `143,184,255`（冰蓝白）/ `63,201,238`（青蓝）/ `178,140,255`（紫罗兰）
      - **引导环也用同一套色相**（逐条行内 `stroke`）→ "哪条轨迹属于哪颗星"一目了然
      - ⚠ `.lm-guide` 的 CSS 里**不能再写 `stroke`**，否则会把逐条的行内颜色盖掉、三条环变成同一个颜色
  - **hover 改为"加速运动"**：时间倍率平滑提到 3.2×；另外仍保留整体放大与"被光标推开"的推斥
  - Logo 下面那个大号渐变「AniHub」标题**已去掉**（用户反馈太占空间，站名在顶栏品牌处已写过）
  - 圆点**不能有 `transform` 过渡** —— 位置每帧都在变，有过渡会让它追不上目标位置、糊成拖影（`.lm-star.is-orbit { transition: none }`）
  - `prefers-reduced-motion` 下 JS 也不跑（CSS 只能停呼吸，位移是 JS 写的）
  - **踩过的坑（都在代码注释里）**：
    1. **CSS `transform` 会完全覆盖 SVG 的 `transform` 属性**。曾把星星位置写进 CSS `transform`，结果属性上的 `translate(...)` 整条失效，星全被摞到原点。现在**定位用外层 `<g transform>` 属性、位移用内层 `.lm-star` 的 CSS transform**
    2. **CSS 自定义属性存数字时是无单位的**：`calc(var(--r) * 0.66 * var(--z))` 这种两个无单位值相乘**不是合法的长度算式**，整条声明会被丢掉（深度缩放一直没生效、`--z` 看着恒定就是这个原因）。改成两个串联的 `scale()`：`scale(calc(var(--r) * 0.66)) scale(var(--z))`
    3. **`transform-box: fill-box` 配 `<rect>` 会把比例算歪**（rect 自带 x/y 偏移），星体被放大到 115 单位而不是 15。改用"以原点为中心的 `<path>` + `scale()` + `transform-box: view-box`"
    4. **柔光不要用实心圆或描边圆环**：实心圆是硬边灰盘、描边圆是粗灰环，都会把星糊住。要用从中心渐隐的径向渐变填充
       - ⚠ 但**"不要光晕"时反过来**：渐变的末端必须是不透明的实色。末端 `stop-opacity=0` 就是那一圈糊边
    5. **离线验证脚本一定要用和组件完全一致的初值**：排查这版时，我的验证脚本把第三个体放在了 `z = 0`（而组件里是 `z = 1.02201`），结果"验证通过"但其实验的是另一条轨道，还因此误判"动量不守恒"。**验证脚本和实现必须共用同一份初值定义**
    6. **"没闭合"要先分清是发散还是整体平移**：一个周期后位置对不上，可能是积分误差（换小步长能救），也可能是质心不在原点（换步长没用、得平移）。这次是后者
    7. **验证周期解至少要跑几十个周期**：只测一个周期的"闭合良好"说明不了任何事 —— 混沌轨道一个周期内可以误差极小、几个周期后就飞出天际。判断"是不是精度问题"的可靠办法是**换一个步长再跑一遍**：若两个步长给出相同的增长率，那就是混沌、不是精度
    8. **不稳定周期轨道要"离线制表 + 按相位播放"**：实时积分会发散，"到点重置"又看得出来在重启；把一个周期预先算成表、之后按相位查表，既永远准确又无缝
       - 接缝要处理**位置**：只在表内相邻两项插值 → 相位回绕时位置跳约 49 单位；跨表尾/表首插值后降到 2.5（正常弧长）
       - ⚠ 但**不要顺手去"修正"尾巴里的点** —— 我加的"旧点追向新位置"补偿把尾巴拽成一团（半径 20.2 vs 真实 28.5），这就是"轨迹线不对"的成因。尾巴只需记录光点去过哪里
       - 验收接缝必须**按稳定的 `is-cN` 类取样**，不能按 DOM 下标（三颗星每帧按 depth 重排，按下标取会量出假的 51 单位跳变）
    9. **批量删代码后要全文搜一遍被删块里定义的常量**：把实时积分换成查表时，我整段删掉了那 123 行，连带删掉了里面的 `const NORM_R`，而它其实被新代码（`PERIOD_TABLE`）用在**模块初始化**阶段 —— 结果整页 `NORM_R is not defined`、直接跳到 `/error/500`
    10. **不要用 `svg.getBBox()` 判断"有没有超出 viewBox"**：它量的是**未裁剪的几何范围**，会把细描边外扩等都算进去，和"视觉上是否出框"不是一回事（实测 bbox 报 67，而几何上最大半径只有约 30）。判断出框要用几何量：`轨迹半径 + 圆点半径 ≤ 32`
    11. **自适应缩放时，`FIT_RADIUS` 要按"最外那层的边缘"留量**：轨迹中心线占 28.5，圆点还要再往外约 0.8，所以不能把 `FIT_RADIUS` 直接设成半宽 30
  - 修法：`--wp-shift` 改为**常量 `24.5px`**，只在首次写入一次，之后不随导航栏高度变化。实测 7 个页面 + 4 种视口宽度（1500/1100/900/700，导航栏高度 49/88/91/91px）下恒为 `24.5px`
  - 代价：导航栏变高时壁纸不再跟着下移半格 —— 本来就只是微调取景，视觉上看不出
- **拓扑页的天幕与全站壁纸像素级一致**：关键是让天幕的**盒子尺寸**、**取景表达式**、**定位基准**三者都与 `.wallpaper-layer` 相同。
  - **`cover` 的缩放比是按「元素自身盒子」算的** —— 这是最容易漏的一点。早期为了盖住筛选条把天幕撑高 44px，`cover` 就把壁纸额外放大约 **5%**（把浏览器分辨率缩到一半时肉眼很明显）。现在天幕用 `position: fixed; inset: 0; height: 100vh`，盒子与壁纸图层**完全相同**（实测 748 vs 748），缩放比精确为 **1.0000**（1500×900 / 1100×700 / 750×450 三种视口均如此）
  - 百分比基准：`fixed` 让 `50%` 以视口为准，与壁纸图层一致（用 `absolute` 时以自身高度为准，会差半格）。祖先用 `overflow-x: clip` 而非 `hidden`——`clip` 不裁剪 fixed 后代，左右方向依然受控
  - 筛选条（`.filter-bar`，y 49..93）**不需要单独处理**：天幕铺满整个视口（0..100vh），它天然就在筛选条之上；筛选条自身透明，于是上下取自**同一层**背景，天然连续
    - ⚠ 曾经有个 `.graph-sky-cap` 想干"盖住筛选条"这件事，但它的 `clip-path: inset(0 0 calc(100% - var(--filter-h)) 0)` 只保留自身顶部 44px，而它 `top: -44px` → 实际可见区是 **y 0..44**，盖的是**顶栏**而不是筛选条。实测把它 `visibility: hidden` 后筛选条像素**完全不变**（`[12,32,59]`），证明它是多余图层；已删除，`--filter-h` 变量也一并去掉。这类"看起来在做某件事、其实没生效"的图层非常误导排查，早删早好
  - 实测：7 个页面在最外侧竖带（版心之外）的像素**完全相同**（取值只有 1 种）
- **通用教训**：CSS 背景的百分比基准是**元素自身**的盒子；`cover`/`contain` 的缩放基准也是**元素自身**的盒子。要让两个尺寸不同的元素显示同一张背景的同一区域，必须让它们的盒子（或视口基准）一致，否则位置和缩放都会差
  1. **`background-color` 画在所有背景图之下**。最初写成 `background-color:#0b1322` + `linear-gradient(rgba(11,19,34,.8) ×2), var(--wallpaper-url)`，那层 80% 渐变把底色整个盖掉，实际呈现的是「壁纸压暗 20%」而非深空色 —— 亮度 29.9，比列表态的深色还亮。于是切换时先变纯色、拓扑挂载后又亮起来，观感就是**背景图闪了一下**。要做纯色就得用**不透明**的面纱；要透壁纸就明确留出透明度。
  2. **`.wiki-view` 的过渡终色必须与天幕最终呈现完全一致**，否则拓扑挂载那一刻会出现亮度台阶。两处统一用 `--graph-veil`（CSS 变量给 `.graph-sky`，JS 侧 `GRAPH_VEIL` 给 `.wiki-view` 的行内样式），实测台阶从 5.7 降到 **1.5**。
  3. **天幕里壁纸的取景必须与 `.wallpaper-layer` 完全一致**（`background-size: cover` + `background-position: center calc(50% + var(--wp-shift))`）。只写 `cover`/`center` 会让同一张壁纸裁到不同位置、亮度对不上，同样产生台阶。
  - 验证提醒：测背景稳定性时**不要取包含星系画布的区域采样** —— 画布逐帧绘制自身光晕会带来约 2~3 的自然波动，会掩盖/混淆背景层的真实表现
  - **筛选条要与拓扑图共用同一层背景（否则割裂）**：`.filter-bar` 位于天幕之上且自身透明 —— 只要它底下不是天幕，就会露出"未被深空面纱压过的原始壁纸"，那一条明显比下方星图区亮。现在靠"天幕铺满整个视口"解决：筛选条正落在天幕范围内，上下来自同一层、天然连续。
    - 关键前提：**`.graph-full` 不能 `overflow: hidden`** —— 那会把 fixed 定位的天幕在新版浏览器里照样裁掉（视情况），露出原始壁纸。改用 `overflow-x: clip` 兜住水平方向
    - 验证手法：**逐个隐藏图层再量像素**。藏掉 `.graph-sky` 后筛选条从 `[12,32,59]` 变 `[12,24,42]` → 说明压暗确实来自天幕；藏掉 `.graph-sky-cap` 毫无变化 → 说明那层是多余的
    - 也不要给筛选条加 `backdrop-filter`：那会把壁纸糊掉，而下方星图区是清晰的，质感依然不一致
    - 定位这类层叠/裁剪问题的可靠手法：**把天幕临时涂成纯品红**，再采样筛选条区域 —— 若读到品红即说明天幕确实在它身后
- **拓扑图切换不卡顿**：three.js 场景是同步重活（建几何体/着色器/上传纹理，实测主线程约 130ms）。现在把它推迟到「列表 ↔ 拓扑」的视图过渡（260ms）跑完之后再构建，避免建场景打断过渡动画 —— 实测切换过程最大帧间隔从 135ms 降到 **33ms**（无 >50ms 卡帧）。另外标签页不可见时停掉帧循环，不在后台空烧 GPU；three.js 分包加载期间显示与深空同色的「拓扑图加载中…」等待态，不再留空
- **从 wiki 切到别的页面不再卡一下**：路由切换用的 `<Transition name="page" mode="out-in">` 会读取**离场元素自身**的 `transition` 属性，取其中**最长**的时长作为离场时间。`.wiki-view` 上曾常驻 `transition: background-color var(--dur-ios-3)`（380ms，用于列表↔拓扑的背景淡入淡出），于是每次离开 wiki 都要等约 400ms 才挂载目标页 —— 实测 `wiki → blog` 内容出现 +404ms，而 `tools → blog` 只要 +179ms。
  - 修法：把那条 transition 收进只在切换瞬间挂上的 `.bg-fading` 类，平时根元素没有 `transition`，离场按 150ms 正常结算。实测 `wiki → blog` 从 **404ms 降到 176ms**，与对照的 179ms 齐平（拓扑模式离开同样从约 400ms 降到 210ms），而列表↔拓扑的背景淡入淡出、深空天幕、`colorScheme` 全部保持正常
  - 通用教训：**不要给路由级视图的根元素常驻 `transition`**，它会污染 `<Transition>` 的时长结算
- **放大后节点文字保持清晰**：节点标签是 Sprite + CanvasTexture，纹理在创建时按固定字号光栅化；透视相机拉近会把这个纹理放大呈现，越近越糊（实测最近机位下标签屏幕宽约 2473px，而纹理只有 564px）。现在按「参照距离 / 相机距离」推算所需的光栅化倍率（上限 4.8，覆盖到 `controls.minDistance`），拉近时按该倍率重画标签纹理 —— 字号、画布、行高同比放大，而精灵的世界尺寸不变，因此**只提升清晰度、不改变视觉大小**。
  - 重画经过 180ms 去抖；**定时器只排一次**：`scheduleLabelRefresh` 每帧都会被调用，若每帧都 `clearTimeout` 再重设，定时器在持续缩放期间永远等不到触发时机（这是第一版没生效的原因）
- **主题开关**：日/夜滑动开关保持 `div` 结构（内部是一整套 em 基准的绝对定位图层，换成原生 `<button>` 会引入 UA 的 padding/字体/行高/垂直对齐，导致太阳、光晕、云朵、星星整体错位约 3px），无障碍语义用 `role="switch"` + `tabindex` + 键盘事件补齐；**双击开关可恢复「按时间自动」**
- **主题云朵漂移**：从「每秒 querySelectorAll + 逐节点写内联 transform」改为 rAF 合并写入，并在标签页不可见 / `prefers-reduced-motion` 时完全停止

### 📖 在线阅读（`/reading`，内部人员及以上）

- **权限**：只对「内部人员（inside）及以上」开放。游客访问会被静默送回主页（与站点其它受限页面一致），**服务端也独立拦了一层**：直接访问 `/books/<id>/xxx.html` 会返回 401，未上架的书返回 404 —— 页面隐藏入口不等于没有权限
- **书架**：按当前身份列出可读的书（封面/书名/作者/体积/简介/标签），点进去即开始阅读
- **阅读器**：书正文以整页 iframe 承载，顶部保留一条紧凑工具条（返回书架 / 书名 / 体积 / 上游来源），书籍自身的目录、全文搜索、回复折叠、字号调节、阅读进度记忆都在书内使用
- **书正文必须用真实 URL 加载（不能用 iframe srcdoc）**：`about:srcdoc` 文档**没有基址**，书里 `href="#章节id"` 这类片段链接会被解析到外层文档的 URL —— 点目录会把 iframe 整个导航成网站首页，书的内容全丢（实测复现：`anchorClick → frameNavigated: http://站点/`）。真实 URL 下片段导航与相对资源都按书的目录正常解析。「解析期就定好主题」则由服务端在 `/books/<id>/<theme>/<entry>` 上注入主题桥实现
- **主题同步**：书内主题以本站为准。注入的主题桥在解析期就把站点主题写进书内文档，并在 `DOMContentLoaded` / `load` 两个阶段再对齐一次 —— 因为内置书有自己的主题状态存在 localStorage，会在加载过程中把它改回去；之后切换主题通过 `postMessage` 同步。切换主题时 iframe 地址里的 `<theme>` 段也会变，因此重载后依然正确
- **上架配置（管理员）**：设置页新增「在线阅读」区块，可逐本勾选「游客可见 / 内部人员可见」，也可整体开关该功能。管理员始终可阅读全部藏书；「游客可见」会自动并入「内部人员可见」；未上架的书对内部人员连元数据都看不到
- **新书如何上架**：把书放进 `public/books/<id>/`，在 `public/books/books.json` 里登记一条（标题 / 作者 / 简介 / 入口文件 / 来源），再到设置页勾选放行身份即可；`books.json` 每次请求实时读取，新增书无需重启

当前已收录：

| 书名 | 内容 | 来源 | 许可 |
| --- | --- | --- | --- |
| 缠中说禅教你炒股票 · 108 课加强版 | 108 课原文（含新浪博客已加密/已删除的文章）+ 2624 条课后回复 + 43 张配图，共 113 篇 | [CONSISTROL/chzhshch-108-plus](https://github.com/CONSISTROL/chzhshch-108-plus) `dc43de3` | MIT |

> 书正文取自上游构建好的单文件电子书 `web/ebook.html`（自包含，配图已内嵌 base64），本项目不对其做修改；来源与版权信息随书放在 `public/books/<id>/SOURCE.md` 与 `LICENSE`。

### ⚡ 加载体验与性能

- **路由懒加载**：所有页面按需下载代码块；`three.js`（3D 二维码 / Wiki 拓扑）、`xterm`（控制台）、TipTap 富文本编辑器等重依赖只在实际打开对应页面时加载，首屏不再一次性下载全部页面
- **右侧细进度条**：贴在网页最右缘的一条细线（常态 3px、悬停 6px）表示当前滚动进度；**只在鼠标移到网页右侧 24px 内时淡入**，平时完全不可见。可直接按住拖动跳转。这条同时承担了页面切换 / 懒加载的进度反馈，因此不再另设顶部横条
  - 判定区刻意做窄（24px）：既不打扰右缘附近的页面元素，也不会在正常浏览时被误触发。判定用 `e.clientX >= window.innerWidth - HOT`，**基准是 `window.innerWidth` 而不是 CSS 视口宽**（本站有 `scrollbar-gutter: stable`，两者不相等）
  - 实现要点：进度用 `transform: scaleY()` 表示（不动画 `height`，避免触发布局）；文档高度由 `ResizeObserver` 缓存，鼠标移动只做几何比较，不在 `mousemove` 里读布局（否则鼠标一动就是一次 reflow）
  - 不需要额外的"热区 div"：可见性由全局 `mousemove` 驱动，多一层空元素只会增加 DOM 与认知负担
  - 游戏页（全屏 iframe）按设计不显示
- **响应压缩（br / gzip）**：应用层用 Node 内置 zlib 做 Brotli / gzip 协商压缩，覆盖 API JSON 与静态文本资源（`three.module` 528KB → **123KB**，`editor` 398KB → **约 96KB**）。生产 Nginx 也开了 gzip，但其 `gzip_types` 不含 `text/javascript`，而 Express 对 `.js` 输出的正是该类型，因此应用层这层兜底是必要的
- **分级缓存**：带内容 hash 的构建产物 `max-age=31536000, immutable`；上传图片 / 壁纸 / 桌宠动画 / 游戏资源 `max-age=604800`；`index.html` 为 `no-cache`（发版即时生效）。压缩响应的 ETag 带编码后缀，条件请求正确返回 304
- **插画资源瘦身**：主页卡片图与错误码插画由 PNG 转 WebP（同画质约 **-78%**，合计 4.9MB → 1.1MB）；favicon 由 864×886 / 929KB 换成 32×32 / 3KB；桌宠召唤图标缩到 256×256。可用 `pnpm optimize:assets` 重新生成（需本机装 ImageMagick；产物已提交，服务器无需安装）
- **壁纸层不再重绘**：全站壁纸由 `background-attachment: fixed` 改为固定定位的独立合成层，滚动时只做合成不重绘；当前壁纸显示后会趁浏览器空闲预热「下次要用的那张」
- **桌宠按需加载**：`Mascot`（774 行 + 全部动作表）改为异步组件，不再进首屏 chunk；动画视频 `preload="none"`，首帧动画在浏览器空闲时才开始拉取
- **完整 HTML 文档加载占位**：Wiki 中带外部 CDN / 脚本的完整 HTML 文档在 iframe 加载期间显示“HTML 文档加载中…”占位，不再一片空白
- **分包优化**：Vue 全家桶单独成块，编辑器 / Markdown 渲染独立分包，静态资源缓存更友好
- **后台标签页不空转**：设置页的监控 / 访问统计轮询在标签页不可见时跳过请求，重新可见时立刻补一次；主题云朵漂移动画同样在不可见时暂停

### 🗓️ Anime（`/anime`）

- **周历视图（默认）**：一次显示一周，7 个等宽大格子，当天所有放送**全部直接显示**（海报 + 标题 + 第 N 话 + 精确时间），不折叠不省略；今天高亮，可前后翻周（范围限制在当前档期覆盖的周内）
  - **每一条是"平铺的一行"，不是浮在列上的圆角卡片**（用户反馈过"row 看起来像圆角矩形悬浮在 col-body 上"）。具体做法：
    - `.col-body` **去掉全部内边距**（原 `padding: 8px`）、行间距 `gap: 7px → 0`
    - `.row` **圆角 11px → 0、边框 → none、背景 → transparent、阴影 → none**，横向铺满整列（实测左右缩进都是 0）
    - 行的 `padding` 只留 `7px 9px`（横向给海报一点呼吸，纵向尽量薄）
    - hover **不再位移/加阴影**（原来 `translateY(-2px)` + 大阴影，是"浮起来"感的主要来源），只改底色
    - 行之间用**伪元素画的 1px 上边线**分隔，而不是 `border-bottom`：一是最后一行不会多出一条悬空的分隔线，二是伪元素能横向铺满整列、不受行内左右内边距影响
  - 手机端（`.day-card-body .row`）本来就是平铺的，这次只是让桌面周历跟它一致
- **月历视图**：整月总览，不同番剧用不同颜色区分，所有条目同样完整显示，带小封面
- **列表视图**：按日期分组展示当月全部放送（海报 + 完整标题 + 时间 + 星期）
- **档期切换**：上一档 / 下一档 / 一键回到当前档期（如 2026 夏季 → 2026 春季），回到当前档期时自动跳到本周
- **语言选择**：动画名支持 中文（默认）/ 日本語 / English / 罗马音 四种显示；语言为中文时详情简介也显示中文（类型标签同步中文化），选择会记住（localStorage）
- **主题切换**：浅色 / 深色 / 自动（按时间，6:00–18:00 浅色、其余深色），右上角**日/夜滑动开关**（移植自 Day-night-toggle-button v4.0：金色太阳弹性滑到右侧变灰月亮（环形山淡入）、星星从上方落下并持续闪烁、云朵下沉、白天光晕圆层视差滑移，纯 CSS 矢量，em 基准缩放），点击整块开关手动切换，默认按时间自动，选择会记住
- **二次元背景**：浅透明二次元美少女图垫底，**自动扫描壁纸目录随机展示**——把任意数量的图片放进 `public/wallpapers/` 即可（PNG / JPG / WebP / GIF），每次进入为下次访问随机预选一张并记住，刷新时首帧直接显示壁纸（不闪纯色背景）、访问中不换图，加载失败自动换下一张，新增图片无需任何登记；也可用环境变量 `WALLPAPER_DIR` 指向其他目录（绝对路径）。**壁纸管理**：管理员可在设置页勾选参与轮播展示的壁纸（缩略图网格 + 全选/全不选，未勾选时自动使用全部，选择持久化到服务端）。无可用图片时自动回退到档期动画的横版高清横幅图（约 1900px）
- **动漫详情**：点击任意条目弹出详情——封面、多语言标题（中文模式下只显示中文标题，不显示英日副标题）、连载状态、类型（中文模式译为中文）、集数、评分、制作公司、中文简介（未收录时回退英文），以及按日期分组的完整放送时间表
- **悬浮提示**：鼠标悬停日历标签显示完整标题 + **大封面预览**（72×102）+ 精确时间
- **当日弹层**：月历视图点击日期格弹出当天完整放送列表（大封面 + 完整标题）
- **数据缓存（服务端）**：档期数据由服务器持久化到 SQLite（`anime_cache` 表）——媒体列表（标题/封面/简介，几乎不变）7 天、排期（每集放送时间，当前档期会随新集公布更新）当前档期 12 小时 / 过去档期 30 天；只重拉过期的部分。浏览器不再直连 AniList，重复打开/切换档期直接读服务器缓存，秒开且不重复请求源站

### 📝 博客（`/blog`）与 📚 Wiki（`/wiki`）

- 登录后即可发布、编辑、删除文章；未登录只读（个人站，不开放注册）
- **三种编辑模式**：编辑页可在 **Markdown**（工具栏 + 源码 + 实时预览）、**所见即所得**（TipTap）、**HTML 源码** 三种模式间切换；Markdown ↔ 富文本互相转换（Markdown 中的字号/颜色等行内 HTML 样式在切换中可保留），模式选择会记住
- **HTML 文档渲染**：Wiki 支持直接保存完整 HTML 文档（`<!DOCTYPE html>` 开头），详情页**不套卡片外壳**，直接在网页主体中按原生样式全宽渲染（保留 `<style>`/`<script>`/外部 CDN 库），仅保留顶部导航；编辑/删除入口在右侧抽屉；普通 HTML 片段与 Markdown 则经 DOMPurify 消毒后内联渲染
- **右侧停靠抽屉（Wiki）**：Wiki 详情页默认不弹出，鼠标移到页面右缘热区时抽屉自动滑出（移开自动滑回；触屏设备用右下角按钮开关），内含 **标题导航** 与 **条目信息**（作者 / 创建 / 更新时间 / 可见性 / 标签）；标题导航随滚动**实时高亮当前所在小节**（Markdown / 富文本 / HTML 片段从渲染结果自动提取，完整 HTML 文档从源码提取；点击平滑滚动定位，iframe 文档通过消息通道定位）
- **编辑工具栏**：Markdown 与富文本模式均提供工具栏，支持对选中文本设置 加粗/斜体/下划线/删除线/标题/行内代码/代码块/引用/列表/链接/字号/颜色 等样式，并可一键清除格式
- **插图**：编辑正文时可上传本地图片（PNG / JPG / WebP / GIF，≤ 8MB），Markdown 模式自动插入图片语法、富文本模式直接插入图片；两种模式都支持 **Ctrl+V 直接粘贴剪贴板中的图片**（复制的图片 / 截图），图片存于 `server/uploads/`
- **游客隐藏（三档可见性）**：编辑页可为每篇文章选择 公开（游客可见）/ 仅内部人员（游客不可见，内部人员可读）/ 仅管理员（私有）三档；权限不足时列表不出现、详情按不存在处理（404），有权限时列表与详情会显示「仅内部可见」「仅管理员可见」标识
- **置顶公告**：登录后在博客列表可对任意文章「置顶」（全局仅一篇），置顶文章排在最前并带 📌 公告 标识，同时作为主页公告展示；再次点击「取消置顶」即可恢复
- 标题自动生成中文 URL 别名（slug），冲突自动加 `-2`
- 支持按关键词搜索标题 / 摘要 / 正文 / 标签，分页浏览
- 正文经 DOMPurify 消毒渲染，防 XSS
- 博客与 Wiki 共用一套编辑系统，`category` 区分

### 🔍 站内搜索（`/search`）

- 导航栏右侧搜索框（或直接访问 `/search`）输入关键词回车，一次搜索博客 / Wiki 文章与动漫
- **文章**：按标题 / 摘要 / 正文 / 标签匹配（复用文章搜索，按身份过滤可见性），结果按 博客 / Wiki 分组展示，置顶公告带 📌 标识
- **动漫**：在服务器已缓存的各档期数据中按标题匹配（罗马音 / 日文 / 英文 / 中文译名），显示封面与所属档期；未访问过动漫页（服务器无缓存）时该部分为空
- 搜索结果按当前身份过滤：游客看不到无权限的文章与动漫（与各页面可见性设置一致）

### 🎮 游戏（`/game`，游客公开）

- **Shattered Pixel Dungeon 网页版**：基于 [shattered-pixel-dungeon](https://github.com/00-Evan/shattered-pixel-dungeon.git) 的 TeaVM/libGDX Web 移植（[shattered-pixel-dungeon-web](https://github.com/glassesmonkey/shattered-pixel-dungeon-web)）
- **玩法**：经典回合制 roguelike 地牢探索——随机地图、职业/天赋、装备/道具、敌人/Boss、商店与深度养成
- **页面集成**：`/game` 路由通过全屏 `<iframe>` 加载 `public/spd/index.html`，纯前端运行，无需后端
- **系统要求**：现代浏览器并启用 WebGL；首次加载需下载较大的 `app.js` 与资源文件
- **加载模式（默认无音频）**：音频资源共 **98 个文件、约 18MB**（31 首 music + 67 个 sounds），因此**默认以无音频模式进入游戏**，不请求任何音频、首次加载明显更快；想要声音时点顶栏「加载音频」按钮，才会从 `/spd/audio-manifest.json` 拉取并写入浏览器缓存。选择会记住
- **无音频为何要服务端配合**：游戏自带的 `?noaudio=1` 只跳过「音乐定时器」，它的批量预载清单 `assets/preload.txt` 里仍然列着全部音频，照样会把 18MB 下完。因此服务端启动时把该清单拆成「完整版 / 无音频版」，并对 `preload.txt` 请求按 `Referer` 里的 `noaudio` 标记返回精简版（响应声明 `Vary: Referer`，避免两种模式在缓存里串味）。实测极速模式音频请求 **98 → 0**、总请求 554 → 388
- **操作**：鼠标点击 / 键盘方向键 / WASD 等，与桌面版一致；手机端可用顶栏「键盘」按钮打开网页内虚拟键盘
- **作弊码**：在游戏内按 `↑ ↑ ↓ ↓ ← → ← → B A B A` 或 `W W S S A D A D B A B A` 可将生命值恢复满（手机端可用虚拟键盘输入）
- **语言**：默认简体中文；可在游戏内设置中切换并记住选择

### 🧰 工具箱（`/tools`，子工具为二级页面）

- **JSON 格式化**（`/tools/json`）：粘贴标准 JSON 一键格式化（缩进美化）或压缩为单行，出错提示定位，结果可一键复制；也能识别**类 JSON 文本**并转换为 JSON——支持 `key = value`、`0x` 十六进制、嵌套 `{}` 的结构体转储（如 DPDK mbuf 调试输出）、`//`、`/* */`、`#` 注释、行尾逗号、匿名块成员提升、裸值数组块（如 `dynfield1 = { 0x0, 0x0 }`），标量值保留原样字符串避免进制/精度丢失
- **二维码解析**（`/tools/qr`）：点击选择 / 拖拽 / Ctrl+V 粘贴二维码图片，自动解析出其中的链接或文本；结果是链接时可一键打开或复制（纯前端解析，图片不会上传）
- **图片裁切**（`/tools/crop`）：上传版图（多格图 / 精灵图），拖动网格线（竖线/横线可**加减**：＋ － 按钮、拖线调整、悬停 ✕ 删除）划分格子，一键裁切成一张张小图；**自动识别**支持**不等分格子**、**粗细框线**与**虚线网格**（分隔线需跨度 ≥92% 图片尺寸：连续线要求无大空隙、虚线要求覆盖率高且断点密集；虚线、多排装饰框堆叠会正确区分；无分隔线时按内容连通域识别并叠加绿框显示，可选**忽略装饰框**——图中的虚线框/实线框描边自动跳过、只保留框内内容）；等分预设支持 2×2~5×5 与**自定义列×行**、透明底 PNG 输出、单张下载或打包 zip（纯前端处理，图片不会上传）
- **图片拼接**（`/tools/splice`）：多张图片（点击多选 / 拖拽 / Ctrl+V 粘贴）拼成一张——**横向 / 纵向 / 网格**三种方向，列表可 ↑↓ 排序、✕ 删除；支持间距（px）、背景色（透明 / 白 / 黑 / 自定义）、条带对齐（顶/中/底、左/中/右）；实时预览并导出原尺寸 PNG（纯前端处理，图片不会上传）
- **HTML 渲染**（`/tools/html-render`）：粘贴 HTML 代码，点击「渲染」（或 Ctrl+Enter）在**新标签页**中原样显示——样式与脚本都正常执行；完整文档原样渲染，片段自动补全为最小页面骨架；内置示例。**一次性渲染**：代码不经过服务器、不写入本地存储，渲染成功后输入框立即清空、Blob URL 随即释放，新标签页关闭后不留任何副本（纯前端处理，代码不会上传）
- **二维码生成**（`/tools/qr-tree`）：输入 URL / 文本，生成**3D 樱花二维码体素树**（three.js，路由懒加载）；树冠为樱花粉云团状体素、树干细而挺拔，树下的**二维码草坪**是只含二维码本身内容的嫩绿色 3D 方块（无静区边距、深色模块微隆可扫码）；3D 视角为 45° 俯视、正面朝向二维码的角；**点击场景**在 3D 树与俯视草坪之间平滑过渡（树从草坪逐层长高 / 收回，俯视只留干净草坪，不再显示黑白二维码或压平层）；修改文本后无过渡直接替换为新树
- **文件对比**（`/tools/compare`）：**文本逐行对比**——左右双栏、增删行高亮（改动块成对对齐）、行号、差异统计（+N/-M）、上一个/下一个差异跳转、交换左右、支持打开文本文件；**文件夹对比**——选择两个文件夹（webkitdirectory），列出 相同 / 不同 / 仅左侧 / 仅右侧 四类（同路径按大小+修改时间判定），带筛选按钮与统计，点击差异文件直接载入文本对比（纯前端处理，文件不上传）

### 🏠 主页（`/`）

- **沉浸式入口**（对齐 Wiki 拓扑的"生动"观感）：
  - 全屏 canvas **星座背景**：带深度的粒子（数量按视口面积自适应，夹在 90~230）＋ 近邻连线 ＋ 鼠标视差 ＋ 粒子被光标吸引。DPR 上限 2；`prefers-reduced-motion` 下只画一帧静态星座；非主页时完全停掉动画循环
  - **星座常驻挂载（`src/components/ConstellationField.vue`，挂在 App 层）**：只切换 `.on` 控制显隐，**绝不随路由销毁重建**。放进 `HomeView` 时每次切回主页都会重新挂载 —— canvas 重建、粒子全部重新随机，观感就是"闪一下然后重新绘制"。粒子场同时是模块级单例（仅视口尺寸变化才重建），因此即使 DOM 被重建也仍是同一片星空
  - **主视觉**：呼吸光球（双环反向旋转）＋ 流动渐变站名 + 公告胶囊 + 「向下探索」提示
  - **入口卡片**：跟手 3D 倾角（`--rx/--ry`）＋ 指向光标处的径向柔光（`--gx/--gy`）＋ 逐张错峰的入场动画
  - 内容区铺一层与主题同色的**长渐变遮罩**：壁纸很花，不压暗卡片文字会被背景吃掉；过渡必须足够长，否则主视觉与内容区之间会出现生硬分界线
- **列表加载骨架屏**（Blog / Wiki 共用 `PostList.vue`）：加载时不再是一行「加载中…」文字，而是**与真实条目同构的占位骨架**（同样的 `.post-item` 外壳与内边距）＋ 逐行错峰的微光扫过。
  - 形状按分类区分：博客是整行条目（2 行摘要 + 右侧箭头），Wiki 是自适应多列卡片（3 行摘要 + 无箭头、更紧凑）—— 尺寸对不上会在内容到达时整页跳动（实测骨架 112px 落在真实卡片 77~124px 区间内）
  - **延迟 150ms 才出现**：命中缓存/本地服务很快时直接出内容，避免"骨架闪一下又消失"的抖动
  - 骨架自身不做入场动画（它是"等待"而非"内容"）。这里有个坑：`.sk-item { animation: none }` 与 `.post-item` 同为**单类选择器、特异性相同**，源序更靠后的 `.post-item` 会胜出 —— 实测骨架条目确实拿到了 `ios-rise-in` 并跟着错峰弹入。必须写成 `.post-items .sk-item` 提高特异性
  - 无障碍上提供 `role="status"` + 视觉隐藏的「加载中…」，`prefers-reduced-motion` 下停掉微光
- **列表逐条入场动画**（`PostList.vue`）：延迟按序号线性递增，并把**总错峰时长**限制在 440ms 内 —— 条目少时接近每条约 80ms 的节奏，条目多时自动收紧到每条约 22ms。
  - 之前是手写 `nth-child(1..4)` 各一个延迟、第 5 条起统一 240ms，于是 Wiki 一页 21 条会"前 4 条依次弹出、剩下 17 条一下全出来"，观感割裂
  - 延迟值由 `staggerDelay(i, total)` 算好后通过 `--stagger` 传给 CSS，不再依赖固定行数；骨架条目不带该变量，因此不受影响
- 顶部显示**公告**：置顶的博客文章会展示在主页（标题 + 摘要，点击进入正文）；未设置置顶时公告自动隐藏。摘要属作者自由填写（可能是装饰符号串），因此限宽 34% 并优先让位给标题
- 入口卡片按身份自动过滤（游客看不到无权限的入口）
- **回到顶部**：任意页面滚动超过一屏后，右下角出现圆形「回到顶部」按钮，点击平滑回到页首
- **错误码页面**：访问有问题时展示对应的错误码插画（`public/http_status_code/`）——游客/内部人员访问需登录的页面（设置/编辑页）显示 **401**，未知路径显示 **404**，也可直接访问 `/error/403` 等查看其他错误码
- **桌宠（蓝毛小女仆）**：网页右下角的 WebM 动画小宠物（按原始项目 [PC2005-cloud/dsh-pet](https://github.com/PC2005-cloud/dsh-pet) 完整适配，包含全部 100 段透明 WebM）。自动行为：动画链按权重切换（待机/转向/漫游/随机动作）、沿底部散步；**左键**点击互动、按住拖动；**右键菜单**：完整动作分类点播 / 随机逗一逗 / 回到角落 / 状态气泡开关 / 本次隐藏；**状态气泡**：互动和随机闲聊时头顶显示文字（可在菜单开关，选择记住）；**默认对内部人员可见、游客不可见**，是否对游客 / 内部人员显示由 设置 → 页面访问权限 中的「桌宠」控制

### ⚙️ 设置（`/settings`，登录后）

- **页面访问权限**：按身份（游客 < 内部人员 < 管理员）配置可见页面。勾选哪些页面允许游客查看；未勾选的页面游客直接访问会被送回主页，导航栏与主页卡片同步隐藏。另可单独授权「内部人员可见页面」（游客不可见但内部人员可看），管理员不受限、全部可见；**Game 游戏** 与 **桌宠** 是两个独立的可见性项
- **网站壁纸**：与页面权限一致的**按身份控制**——勾选「游客可见壁纸 / 内部人员可见壁纸」（管理员登录后恒可见），壁纸取自服务器默认目录（`public/wallpapers` 或环境变量 `WALLPAPER_DIR`）
- **Anime 成人内容**：同样**按身份控制**——勾选「游客可见成人内容 / 内部人员可见成人内容」（默认仅管理员可见），决定 Anime 日历与站内搜索是否展示标注为成人的番剧
- **服务器监控**：设置页底部实时显示服务器状态（每 5 秒自动刷新，仅管理员可见）——运行时长、收到请求数、系统内存 / 进程 RSS / 堆内存占用、磁盘使用容量 / 系统负载、CPU 核数、Node 版本、系统平台、主机名、数据库大小（含 WAL）与文章数；百分比使用率（系统内存 / 本进程 RSS / 磁盘容量）使用**动态液体液面卡片**展示，不同卡片使用不同颜色；并有**监控图表**：CPU 使用率、内存使用、网络带宽（入网/出网）、系统盘读写，**时间范围可选 小时 / 天 / 周 / 月 / 自定义**（悬停显示各序列数值）。指标由后端每 5 秒采样（CPU/内存用 node:os；网络 Linux 读 /proc/net/dev、Windows 用 netstat；磁盘 Linux 读 /proc/diskstats、Windows 用 Get-Counter）存入 SQLite，保留 35 天，按范围自动分桶聚合
- **访问统计**：设置页新增“访问统计”（仅管理员可见）——总访问量 / 今日 / 昨日 / 独立 IP、近 30 天访问趋势、热门页面与热门 IP、按时间倒序的访问记录（IP、IP 归属地、路径、来源页、User-Agent），支持按 IP / 路径 / UA 搜索与分页；**点击热门页面**可查看访问过该页面的 IP 列表（按访问次数排序，支持分页与继续查看 IP 详情）；**点击任意访问记录行**可查看该条完整详情（完整路径 / Referer / UA 等），点击 IP 可继续打开 **IP 来源详情**（完整归属地、ISP、经纬度、访问汇总、路径/UA 分布与最近访问记录）；并内置 **IP 来源地图**（Leaflet + OpenStreetMap），用圆点标注访问量、热力图显示热点区域；热力层不会遮挡圆点点击，点击圆点弹窗后还可继续点击“查看 IP 详情”查看该热点下的 IP 列表。生产模式由 Express 记录整页加载，SPA 内部路由切换由前端自动上报，避免重复计数；IP 归属地使用 ip-api.com 免费接口按需异步解析并缓存到 SQLite（含经纬度）
- **管理员控制台**（`/console`，登录后导航栏入口）：**多终端标签页**（＋新建 / ✕关闭，后台标签页会话保持运行，与 Xshell 一致）；每个标签页是**真正的终端模拟器（xterm.js）**——`htop`/`vim`/`top` 等全屏程序正常显示（Linux 下经伪终端 PTY + 原始字节流，ANSI 由终端层解析）；**bash 原生能力**：Tab 补全、提示符、方向键历史、Ctrl+C 中断、Ctrl+D 退出 shell、cd 均与本地终端一致；**Ctrl+C / ⏹ 停止**中断（PTY 发 SIGINT，真终端行为，连续 3 次强制 SIGKILL；超时硬杀兜底）；**终端内查找**（Enter 下一个 / Shift+Enter 上一个，显示匹配位置）；2000 行滚动回看；会话退出后可一键重启；全宽全高铺满页面（服务器日志直接在终端里 `journalctl -u anihub` / `tail -f` 查看）。仅管理员可用：路由守卫 401 + 服务端 `authRequired` + WebSocket token 三重校验。**提权**：部署脚本给运行用户配了免密 sudo（`/etc/sudoers.d/anihub`），控制台里可 `sudo -i` 进 root shell、`sudo bash deploy/update.sh` 一键更新网站（见下方部署章节）。**本地 Web 访问**：控制台新增「本地 Web」标签页，自动扫描服务器本机监听的 TCP 端口并列出，也可手动输入 `127.0.0.1:端口` / `localhost:端口/路径`；点击后在**新标签页**通过本站反向代理打开（仅允许回环地址，避免 SSRF；自动注入/改写 HTML `<base>`、改写 JS 中常见 `baseURL`、注册 Service Worker 把页面发出的根路径请求改写到代理前缀，支持 Vue Router 等 history 路由的 SPA）。
- **匿名模式（内部人员身份）**：只读的中间身份。点击顶栏「键盘」按钮打开网页内虚拟键盘，输入 `inside` 并回车即获取（口令见 `server/.env` 的 `INSIDER_KEYWORD`，默认 `inside`）。顶栏**只显示一个圆形头像**，悬停时头像放大（1.55×）并在下方浮出卡片，卡片标题为「匿名模式」＋「退出内部模式」按钮（与 B 站头像的交互一致）。要点：
  - 放大方向取 `transform-origin: right top` —— 这个属性指的是"**固定不动的那一点**"：固定**右上角**，头像就向**左下**方向变大（实测右边缘/上边缘 Δ=0.0px，左边缘 −15.4px、下边缘 +15.4px）
  - **卡片按"放大后头像的视觉中心"对齐，而不是 `.insider-wrap` 的布局中心**：`transform: scale()` 只改变绘制、**布局盒子尺寸不变**，所以放大后的视觉中心比布局中心左移 (28 × 0.55) / 2 ≈ 7.7px。卡片若简单按 `left: 50%` 居中就会整体偏右（实测偏 15px）。修法是 `left: calc(50% - 7.7px)`（变量 `--ic-shift`），实测卡片中心与头像视觉中心偏移为 **0px**
  - 卡片纵向位置取 `top: calc(100% + 13px)`：头像放大后底边下移约 15px，卡片放在"放大后底边上缘 2.6px"处 —— 收起态卡片在头像下方（视觉干净），**展开态与头像相接**，保证「头像 → 卡片」的悬停路径连续（两者是同级子元素，一旦经过空隙就会触发 `pointerleave` 把卡片收起），同时那 2.6px 落在头像下缘空白处、不会盖到脸
  - 卡片用 `visibility` 而非仅 `opacity` 收起 —— 否则收起状态下顶栏的可达性树里仍会带上卡片文字
  - **卡片要浮在顶栏其它部分之上，只给卡片设 `z-index` 是不够的**：`.nav-search` 是 `position: relative`，而 `.nav-end`（卡片的祖先）是 `static` + `z-index: auto`；按 CSS 绘制顺序"定位元素"会盖过"非定位元素的子元素"，实测卡片底部被搜索框压住。修法是把 `.nav-end` 也提为定位元素 + `z-index: 2`
  - **顶栏整体层级必须高于页面里的悬浮控件**：导航栏因 `backdrop-filter` 自建层叠上下文，内部元素再高的 `z-index` 也出不去，能否压住别人完全取决于 `.navbar` 自己的 `z-index`。wiki 页的「列表 / 拓扑图」切换器是 `z-index: 220`，而 `.navbar` 原值 50 —— 会被它压住。现取 **250**：高于页面级浮层（220 及以下），低于所有弹窗层（300+ 详情弹窗、400 虚拟键盘、1000 文件管理），因此不会挡模态。**调整前请确认 221~299 这段区间仍无其它用途**
  - 退出按钮与卡片同色系（半透明面板色 + 常规边框），不用实心色块抢视觉焦点
  - `:focus-within` 让键盘 Tab 可达；触屏用 click 切换（`:hover` 在触屏上不可靠）
  - 顶栏任何位置都不出现「匿名模式」「内部人员模式」等文字（只在浮层卡片里），回归断言守着这一点
  - 内部人员能看到游客看不到的页面与文章，但不能编辑、不能进设置页；**进入匿名模式后全站页面都显示壁纸背景**（与 Anime 页共用同一套壁纸与缓存）
- **隐藏登录入口**：界面不显示任何登录按钮，点击顶栏「键盘」按钮打开网页内虚拟键盘，输入 `login` 并回车弹出登录框（Esc 或点击遮罩关闭）；已登录右上角显示当前站点版本号与提交 ID（格式 `版本号.提交ID`）以及 设置 / 退出 按钮

## 环境要求

- [Node.js](https://nodejs.org) ≥ 20.19（推荐最新 LTS 或 v24，本仓库在 Node 24 上开发验证）
- [pnpm](https://pnpm.io)（依赖管理；未安装时执行 `npm i -g pnpm@11`，Node 自带的 npm 仅用于安装 pnpm）
  - 新增依赖若带 `postinstall` 等构建脚本，pnpm 默认不执行并报 `ERR_PNPM_IGNORED_BUILDS`，需在 `pnpm-workspace.yaml` 的 `allowBuilds` 里显式放行（当前只放行了 `esbuild`）

## 快速开始（开发模式，双进程）

```bash
# 1. 安装依赖
pnpm install

# 2. 一键启动前后端（推荐）
pnpm dev:all
# Windows 也可以直接双击 dev.bat；Linux/macOS 可执行 bash dev.sh
```

上面命令会同时启动：

- 后端 API：`http://localhost:3001`（`--watch` 自动重启）
- 前端 Vite：`http://localhost:5173`

按 `Ctrl+C` 会同时停止前后端。

如果不想用一键脚本，也可以手动分两个终端：

```bash
# 终端 A：启动后端 API（端口 3001，--watch 自动重启）
pnpm dev:server

# 终端 B：启动前端开发服务器
pnpm dev
```

打开终端 B 显示的地址（默认 <http://localhost:5173>）。开发模式下 Vite 会把 `/api` 请求代理到后端（见 `vite.config.js`）。

首次启动后端会自动创建 `server/anihub.db`（SQLite）并建表，同时按 `server/.env` 中的配置自动创建站点账号（不存在则新建，已存在则同步为当前密码）。

**站点账号**（个人站，不开放注册）：编辑 `server/.env` 设置 `ADMIN_USERNAME` / `ADMIN_PASSWORD`，改完重启即生效；未设置时默认 `admin` / `anihub-dev-password`（生产环境务必修改）。

**内部人员口令**：`server/.env` 的 `INSIDER_KEYWORD`（默认 `inside`）为内部模式口令；点击顶栏「键盘」按钮打开网页内虚拟键盘，输入 `inside` 后回车即可触发。若修改该口令，需同步更新 `src/App.vue` 中触发时发送的关键词（默认写死 `inside`）。

## 生产部署（单端口）

```bash
# 1. 构建前端
pnpm build

# 2. 启动服务（Express 同时托管 API 与 dist/ 静态文件）
pnpm start
```

访问 <http://localhost:3001> 即可，全部功能同一端口。

> 生产环境建议设置环境变量 `JWT_SECRET`（登录令牌签名密钥）与登录密码 `ADMIN_PASSWORD`；不设置则用默认开发值。

### 云端部署（推荐：轻量云服务器 / VPS）

本项目使用 Node 内置 `node:sqlite` 与本地磁盘（SQLite 数据库 + 上传图片），**必须运行在带持久磁盘的常驻服务器上**（轻量服务器 / VPS / 容器平台均可；纯静态托管与 Serverless 不适合，函数环境的文件系统不持久）。

`deploy/` 目录已备好全套部署文件，适用于 Ubuntu 22.04 / 24.04 的国内轻量服务器（阿里云 / 腾讯云轻量应用服务器，2C2G 即可）：

| 文件 | 作用 |
|---|---|
| `deploy/setup.sh` | 一键部署：装 Node 24 与 pnpm → 拉代码（默认走 GitHub 代理）→ 构建 → 生成 `.env`（随机密钥）→ systemd 守护 → Nginx + HTTPS → 定时备份 |
| `deploy/update.sh` | 一键更新：拉代码（默认走 GitHub 代理）→ `pnpm install --frozen-lockfile` → 构建 → 重启服务 → 同步 Nginx 配置（自动解析域名，`nginx -t` 失败自动回滚）；以 root 运行时自动把应用目录所有权归还给运行用户 |
| `deploy/anihub.service` | systemd 服务单元（开机自启、崩溃自动重启；不做提权类加固，以支持控制台内 su/sudo） |
| `deploy/anihub.nginx.conf` | Nginx 反向代理（含 WebSocket 转发）+ Let's Encrypt HTTPS |
| `deploy/backup.sh` | 数据备份（SQLite + 上传图片，保留最近 14 份，每日 03:00） |
| `deploy/crontab.example` | 定时备份任务示例 |

> 大陆服务器直连 GitHub 不稳定，`setup.sh` / `update.sh` 默认使用第三方代理 `https://ghfast.top/https://github.com/CONSISTROL/anihub.git` 拉取；代理域名失效时，把脚本里 `REPO_URL` / `PROXY_URL` 换成 `gh-proxy.com`、`mirror.ghproxy.com` 等即可（也可通过环境变量 `REPO_URL` 覆盖为 Gitee 地址）。

快速上手（服务器上执行）：

```bash
# 方式一：脚本自动部署（默认从 GitHub 代理拉取代码，无需指定 REPO_URL）
sudo DOMAIN=anime.example.com CERT_EMAIL=you@example.com \
  ADMIN_USERNAME=admin ADMIN_PASSWORD=你的密码 \
  bash deploy/setup.sh

# 方式二：代码已上传到服务器（如 scp 到 /opt/anihub）后执行
sudo DOMAIN=anime.example.com CERT_EMAIL=you@example.com bash /opt/anihub/deploy/setup.sh
```

部署完成后访问 `https://你的域名`，点击顶栏「键盘」按钮打开网页内虚拟键盘，输入 `login` 并回车弹出登录框，输入管理员账号登录。日常更新代码：

```bash
bash /opt/anihub/deploy/update.sh
```

`update.sh` 的同步策略：本地历史可快进且工作区干净时用 `git merge --ff-only`；如果本地提交记录与远程不一致（分叉）或存在未提交改动，会以远程为准执行 `git clean -fd && git reset --hard FETCH_HEAD`，因此服务器上的本地独有提交会被丢弃。`.env`、数据库、上传图片等被 `.gitignore` 排除的文件不受影响。

管理员也可以在**设置页 → 网站升级**中查看当前版本、远程最新版本、落后多少个提交，并输入 su/root 密码一键触发升级；升级过程中会显示实时进度条与构建日志，服务重启后自动标记完成。


**在网页控制台里更新网站**：部署脚本已给运行用户（默认 `anihub`）配免密 sudo，控制台内执行：

```bash
sudo bash deploy/update.sh   # 一键更新（拉代码 → 构建 → 重启 → 同步 nginx）
sudo -i                       # 或直接进 root shell（su 需 root 密码，sudo 免密）
```

**安全提醒**：首次部署请务必在 `server/.env` 中设置强随机 `JWT_SECRET`（部署脚本已自动生成）、修改 `ADMIN_PASSWORD`；如需修改内部人员口令 `INSIDER_KEYWORD`，同步改 `src/views/HtmlRenderToolView.vue` 中触发时发送的关键词后重新构建。

## 项目结构

```
├── index.html                    # 入口页面（图标、theme-color、首帧前主题/壁纸恢复脚本）
├── package.json                  # 依赖与脚本（packageManager 锁定 pnpm；pnpm optimize:assets 可重新生成插画资源）
├── pnpm-lock.yaml                # 依赖锁定文件（部署用 pnpm install --frozen-lockfile）
├── pnpm-workspace.yaml           # pnpm 设置（构建脚本白名单 allowBuilds，如 esbuild）
├── vite.config.js                # dev proxy: /api → :3001；生产分包（editor/markdown）
├── scripts/
│   └── optimize-assets.mjs       # 静态插图优化：PNG → WebP、生成 favicon / 主屏图标
├── public/books/                 # 在线阅读藏书
│   ├── books.json                # 书目清单（标题/作者/简介/入口/来源），每次请求实时读取
│   └── <id>/                     # 每本书一个目录：正文（单文件 HTML）+ SOURCE.md + LICENSE
├── deploy/                       # 云端部署（轻量服务器）：setup.sh / systemd / nginx / 备份
├── game/                         # 游戏说明与重新构建指引（Shattered Pixel Dungeon Web）
├── public/spd/                   # SPD Web 静态构建产物（/game 用 iframe 加载 /spd/index.html）
├── server/                       # Node + Express + SQLite 后端
│   ├── index.js                  # 装配：压缩 → JSON → API 路由 → 静态托管 dist/（分级缓存）→ SPA fallback → 优雅退出
│   ├── db.js                     # node:sqlite 连接 + 建表（WAL + synchronous=NORMAL 等调优，含各类列迁移）+ WAL checkpoint/过期清理
│   ├── config.js                 # PORT / JWT_SECRET / INSIDER_KEYWORD（读环境变量）
│   ├── routes/auth.js            # 登录 / 内部人员口令 / 获取当前用户（bcrypt 异步比对 + 失败限流）
│   ├── routes/posts.js           # 文章 CRUD（列表/详情/新建/编辑/删除，作者校验，md/html 双格式，三档可见性，置顶公告）
│   ├── routes/settings.js        # 站点设置（页面可见性 / 壁纸 / 成人内容开关）
│   ├── routes/anime.js           # Anime 档期数据：SQLite 缓存 + 回源 AniList + 已缓存数据搜索
│   ├── routes/upload.js          # 图片上传（PNG/JPG/WebP/GIF → server/uploads/）
│   ├── routes/wallpapers.js      # 壁纸目录扫描（GET /api/wallpapers → 图片 URL 列表）
│   ├── routes/monitor.js         # 服务器监控（实时状态 + 历史图表分桶查询）
│   ├── routes/console.js         # 管理员控制台（执行命令 / 查看服务端日志）
│   ├── routes/visits.js          # 访问统计（内存缓冲批量落库 + 有上限的 IP 归属地解析）
│   ├── routes/reading.js         # 在线阅读接口（按身份过滤 + 上架配置 + /books 会话 Cookie）
│   ├── books.js                  # 书库：清单读取、上架配置、/books 目录的鉴权中间件
│   ├── routes/localWeb.js        # 本地 Web 代理（会话 Cookie / 本机端口扫描 / 回环反向代理）
│   ├── logger.js                 # console 输出捕获（环形缓冲，供控制台查看）
│   ├── monitorCollector.js       # 指标采集器（每 5s 采样 CPU/内存/网络/磁盘 → SQLite metrics）
│   ├── consoleSocket.js          # 控制台 WebSocket（背压保护 + 心跳 + 退出时关闭）
│   ├── uploads/                  # 上传的图片（gitignore，运行时生成）
│   ├── middleware/
│   │   ├── auth.js               # JWT 鉴权：authRequired（仅管理员）/ optionalAuth（游客/内部/管理员）
│   │   ├── compress.js           # 响应压缩（br/gzip 协商，流式 + 背压 + 编码感知 ETag/304）
│   │   └── rateLimit.js          # 内存限流（固定窗口 / 失败计数封禁）
│   └── lib/                      # slugify（保留中文）、validate、clientIp（可信客户端 IP 解析）
└── src/
    ├── main.js                   # 应用入口（全局错误兜底 + 挂载 router）
    ├── App.vue                   # 布局壳：NavBar + router-view（桌宠异步加载）
    ├── style.css                 # 全局样式、深浅主题与语义色 CSS 变量
    ├── router/index.js           # 路由表 + 登录守卫
    ├── api/
    │   ├── http.js               # fetch 封装（/api 前缀、Bearer、401 自动登出）
    │   ├── posts.js              # 文章接口（含置顶 / 公告）
    │   ├── settings.js           # 设置接口
    │   ├── localWeb.js           # 本地 Web 访问接口（扫描/会话）
    │   └── anilist.js            # Anime 服务端缓存接口封装（/api/anime）
    ├── composables/
    │   ├── useAuth.js            # 登录状态（管理员 token + 内部人员 token，localStorage 持久化）
    │   ├── useSeason.js          # 档期状态：加载数据、切档、翻月
    │   ├── useLanguage.js        # 显示语言状态（默认中文，持久化）
    │   ├── useTheme.js           # 主题状态：浅色/深色/按时间自动（时间边界到点即生效）
    │   ├── useSettings.js        # 站点设置状态（游客/内部人员可见页面，全局单例 + 单次请求）
    │   └── useWallpaper.js       # 壁纸管理器（Anime 页与内部模式全局背景共用，含空闲预热）
    ├── utils/
    │   ├── date.js               # 档期映射、日历网格、时间格式化
    │   ├── titles.js             # 按语言解析标题（titleFor）
    │   └── jsonLike.js           # 类 JSON 解析（C 结构体转储 → JSON）
    ├── data/
    │   ├── zhTitles.js           # 中文标题映射表（AniList id → 译名）
    │   └── zhDescriptions.js     # 中文简介映射表
    ├── views/
    │   ├── HomeView.vue          # /           导航卡片主页（含公告）
    │   ├── CalendarView.vue      # /anime      Anime 日历
    │   ├── BlogListView.vue      # /blog       博客列表
    │   ├── BlogPostView.vue      # /blog/:slug 博客详情
    │   ├── WikiListView.vue      # /wiki       Wiki 列表
    │   ├── WikiPostView.vue      # /wiki/:slug Wiki 详情
    │   ├── EditView.vue          # 新建/编辑（博客与 Wiki 共用，Markdown/富文本双模式）
    │   ├── LoginView.vue         # 登录
    │   ├── SettingsView.vue      # /settings 设置（页面访问权限）
    │   ├── SearchView.vue        # /search 站内搜索（文章 + 动漫）
    │   ├── HttpErrorView.vue     # /error/:code 错误码页面（401/404 等插画）
    │   ├── GameView.vue          # /game 游戏页（iframe 嵌入 SPD Web）
    │   ├── ToolsView.vue         # /tools 工具箱首页（子工具入口）
    │   ├── JsonToolView.vue      # /tools/json JSON 格式化（含类 JSON 解析）
    │   ├── QrToolView.vue        # /tools/qr 二维码解析
    │   └── CropToolView.vue      # /tools/crop 图片裁切（网格线切图）
    │   └── SpliceToolView.vue    # /tools/splice 图片拼接（横向/纵向/网格合成）
    │   └── HtmlRenderToolView.vue # /tools/html-render HTML 渲染（新标签页显示）
    │   └── QrTreeToolView.vue     # /tools/qr-tree 二维码生成（three.js 懒加载）
    │   └── CompareToolView.vue    # /tools/compare 文件对比（文本 diff + 文件夹对比）
    └── components/
        ├── NavBar.vue            # 全站导航栏（含登录态与内部模式徽标）
        ├── BackToTop.vue         # 一键回到顶部按钮（全站右下角）
        ├── Mascot.vue            # 桌宠（蓝毛小女仆）：右下角 WebM 动画小宠物（完整动画链/动作菜单/互动/拖动）
        ├── MarkdownView.vue      # Markdown 渲染（marked + DOMPurify）
        ├── RichTextView.vue      # 富文本 HTML 渲染（DOMPurify）
        ├── RichTextEditor.vue    # 所见即所得编辑器（TipTap，含样式工具栏）
        ├── MdToolbar.vue         # Markdown 模式编辑工具栏
        ├── PostList.vue          # 文章列表（搜索/分页，博客/Wiki 共用；博客支持置顶公告）
        ├── PostDetail.vue        # 文章详情（作者操作，按格式渲染）
        ├── WeekView.vue          # 周历视图（默认，大格子全部显示）
        ├── Calendar.vue          # 月历视图（含悬浮提示、当日弹层）
        ├── DayPopover.vue        # 当日放送列表弹层
        ├── ListView.vue          # 列表视图（按日期分组）
        ├── AnimeBackground.vue   # 浅透明二次元美少女背景（Anime 页，含横幅回退）
        ├── InsiderBackground.vue # 内部模式全局背景（inside 模式下全站壁纸）
        ├── SeasonSwitcher.vue    # 档期切换按钮
        ├── LanguageSelector.vue  # 语言选择下拉框
        ├── ThemeSelector.vue     # 日/夜滑动主题开关（移植 Day-night-toggle-button）
        └── AnimeDetail.vue       # 动漫详情弹窗
```

## 工作原理

1. 按当前日期计算所在档期（冬 1–3 月 / 春 4–6 月 / 夏 7–9 月 / 秋 10–12 月）
2. 查询 AniList 获取该档期全部动画（`season` + `seasonYear`，按热度排序）
3. 再用这批动画的 ID 查询 `airingSchedules`，得到**每集**的精确放送时间（Unix 时间戳）
4. 结果按档期缓存到服务器 SQLite（媒体列表 7 天 / 排期当前档期 12 小时、过去档期 30 天，分开缓存只重拉过期部分），期间重复加载/切换档期直接读服务器缓存，不重复请求源站
5. 时间戳转本地时区后填入周历 / 月历网格

## 性能与运维说明

以下是本站在加载与运行时做过的主要优化，改动集中在少数几个文件，便于按需回退。

### 传输层（`server/middleware/compress.js`、`server/index.js`）

| 项 | 说明 |
|---|---|
| 压缩 | 应用层用 Node 内置 `zlib` 实现 br / gzip 协商，流式压缩 + 背压透传。**必须放在所有路由之前挂载**，否则包不住 `res.write`/`res.end` |
| 为何不交给 Nginx | Nginx 配置里的 `gzip_types` 不含 `text/javascript`，而 Express 5 对 `.js` 输出的正是该类型 —— 生产环境前端 chunk 实际是未压缩的。应用层这层兜底同时覆盖 dev（Vite 代理）与生产（Nginx 反代） |
| 跳过规则 | 小于 1KB、大于 8MB、图片/视频/字体、`206` 分段响应、已带 `Content-Encoding`、`HEAD` 请求一律放行 |
| 缓存 | `/assets/*-<hash>.js|css` → `max-age=31536000, immutable`；`/uploads`、`/wallpapers`、`/pet`、`/spd` → `max-age=604800`；`index.html` → `no-cache` |
| 条件请求 | 压缩响应的 ETag 会带 `-br` / `-gzip` 后缀（同一 URL 不同编码内容不同），并据此正确返回 304 |

### 数据库（`server/db.js`、`server/routes/visits.js`）

- `PRAGMA synchronous = NORMAL`：WAL 模式下避免「每个页面访问都 fsync 一次」；另有 `busy_timeout`、`wal_autocheckpoint`、`journal_size_limit`、64MB 页缓存
- 访问记录改为**内存缓冲 + 定时批量落库**（满 50 条或 3 秒触发），并设队列上限，异常刷量时不会撑爆内存
- 每 5 分钟 `wal_checkpoint(TRUNCATE)`，每小时清理过期访问记录（默认保留 180 天）；进程退出前也会 checkpoint 并 `db.close()`
- 启动时只在 `.env` 里的密码与库中 hash 不一致时才重算 bcrypt（原先每次启动都无条件算一次）

### 安全（`server/lib/clientIp.js`、`server/middleware/rateLimit.js`、`server/middleware/auth.js`）

- **客户端 IP 取可信来源**：Nginx 用 `$proxy_add_x_forwarded_for`，XFF 的**第一段是请求方可以随便伪造的**。现在统一走 `app.set('trust proxy', 1)` 提供的 `req.ip`，兜底解析也取 XFF 末段。（原先取首段，可被用来绕过限流、伪造访问归属地并触发大量外呼）
- 登录失败限流（15 分钟 8 次封禁）、内部人员口令限流、IP 归属地解析并发上限（2）与队列上限
- `bcrypt.compare` 改为异步；用户不存在时也跑一次等长比对，避免用响应时间探测用户名
- 关闭 `X-Powered-By`

### 前端（`src/`）

- 主题：`resolved` 依赖一个每分钟推进的 tick，**跨 6:00 / 18:00 会自动切换**（原实现用 computed 包 `new Date()`，结果被永久缓存，过点不生效）；手动切换后出现的「自动」小按钮可随时切回
- 壁纸：改为固定定位合成层（不再用 `background-attachment: fixed`），并在当前壁纸显示后空闲预热下一张
- 桌宠：`Mascot` 异步加载 + 视频 `preload="none"` + 空闲时起播；召唤按钮在「有权限但未显示」时一定可点
- 滚动/尺寸监听：滚动指示条不再在每次 `mousemove` 里读 `scrollHeight`（改用 `ResizeObserver` 缓存）；导航栏写 `--nav-h` / `--wp-shift` 前先比较值
- 全局错误兜底：`app.config.errorHandler` 把未捕获异常导向 500 错误码页，不再是白屏

### 插画资源（`scripts/optimize-assets.mjs`）

`pnpm optimize:assets` 会：把 `public/home/*.png`、`public/http_status_code/*.png` 转成 WebP（同画质约 -78%）；用 `public/anihub.png` 生成 `favicon-32.png` 与 `apple-touch-icon.png`；把桌宠召唤图标缩到 256×256。需要本机装 ImageMagick（Windows 常见路径会自动探测，也可用 `MAGICK_PATH` 指定）。**产物已提交进仓库，服务器上不需要装 ImageMagick。**

> `public/wallpapers/` 是用户自己的壁纸原图，不在本脚本处理范围内。

## 已知说明

- **本地 Web 代理是路径前缀反向代理**（`/local-web/http/<host>:<port>/...`），适合纯静态页面、支持子路径部署的服务和简单 HTTP 服务；如果目标服务硬编码从根路径发起 `/api` 等请求且不支持配置 base URL，可能需要先在服务自身配置子路径前缀，或使用控制台/SSH 做端口转发访问。
- **中文标题与中文简介为人工维护的映射表**（AniList 不提供中文标题/简介字段）：标题见 [src/data/zhTitles.js](src/data/zhTitles.js)、简介见 [src/data/zhDescriptions.js](src/data/zhDescriptions.js)、类型标签中文翻译见 [src/data/zhGenres.js](src/data/zhGenres.js)，均完整覆盖 2026 夏季档全部正常向作品（成人向除外）；语言为中文时点开详情会优先显示中文简介，未收录的动画回退显示罗马音标题与英文简介。新增条目时在文件中按 `AniList id: '内容'` 追加即可（id 可在动画详情弹窗的 AniList 链接中查到）
- 档期内已完结 / 未开播 / 缺排期的动画不出现在日历上，会列在日历下方
- 日历数据由服务器按需回源 AniList 并缓存（外网需可达）；首次请求或缓存过期时服务器拉取，之后页面只读服务器缓存。AniList 官方故障时若服务器已有缓存仍可正常显示，无缓存时日历会显示错误提示，其余页面不受影响
- 后端使用 Node 内置 `node:sqlite`（Node 24+ 稳定，20/22 为实验特性）；若 Node 版本过低请升级
- **站点壁纸建议压缩后再放入 `public/wallpapers/`**：壁纸会以原图尺寸下载，4K 无损 PNG 单张可达十几 MB。建议压到 1920px 宽的 WebP / JPG（几百 KB），首屏打开会快很多
- **在线阅读的安全边界**：`/books` 目录由 `server/books.js` 的 `booksGuard` 先鉴权再放行，响应带 `Cache-Control: private, no-cache` + `Vary: Cookie`，避免 CDN/共享缓存把某本书串给其它身份。书正文走 **srcdoc** 渲染，因此是不透明来源（读不到父文档），主题值由父页面直接写进注入脚本、后续变化用 `postMessage` 同步
- **不要让 vite 直接服务 `/books`**：`vite.config.js` 里已把 `/books` 代理到后端；若绕过（例如让 vite 直接读 `public/`），本地的身份校验会被绕过
- 反代部署时请保留 `proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for`（`deploy/anihub.nginx.conf` 已配置）——服务端依赖它还原真实客户端 IP；同时不要在前面再叠加未经配置的可信代理层，否则 `trust proxy` 的层数需要相应调整
